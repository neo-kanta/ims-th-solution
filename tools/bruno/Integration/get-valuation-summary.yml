info:
  name: get-valuation-summary
  type: http
  seq: 12

http:
  method: GET
  url: "{{base-url}}integration/dashboard/valuation-summary?scope=company"
  params:
    - name: scope
      value: company
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
