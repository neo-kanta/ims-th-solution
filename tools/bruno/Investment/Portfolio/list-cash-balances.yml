info:
  name: list-cash-balances
  type: http
  seq: 15

http:
  method: GET
  url: "{{investment-base-url}}/portfolios/:portfolioId/cash-balances"
  params:
    - name: portfolioId
      value: "{{investment-portfolio-id}}"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
