info:
  name: get-approval-settings
  type: http
  seq: 8

http:
  method: GET
  url: "{{base-url}}workflow/settings"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
