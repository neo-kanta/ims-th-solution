info:
  name: disable
  type: http
  seq: 4

http:
  method: POST
  url: "{{iam-mfa}}/disable"
  body:
    type: json
    data: |-
      {
        "totp_code": "{{totp_code}}"
      }
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.getStatus() === 204) {
          bru.setEnvVar("mfa-enabled", "false", { persist: true });
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
