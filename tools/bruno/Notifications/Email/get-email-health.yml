info:
  name: get-email-health
  type: http
  seq: 4

http:
  method: GET
  url: "{{notification-base-url}}/email/health"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
