info:
  name: get-holdings-valuation
  type: http
  seq: 34

http:
  method: GET
  url: "{{investment-base-url}}/funds/:fundId/holdings/valuation"
  params:
    - name: fundId
      value: "{{investment-fund-id}}"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
