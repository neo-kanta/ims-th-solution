info:
  name: provider-health
  type: http
  seq: 4

http:
  method: GET
  url: "{{market-data-base-url}}/provider-health"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
