info:
  name: list-feeds
  type: http
  seq: 5

http:
  method: GET
  url: "{{integration-etl}}/feeds"
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();
        const data = body?.data || [];

        if (data[0]?.id) {
          bru.setEnvVar("feed_id", data[0].id, { persist: true });
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
