info:
  name: delete-research-report
  type: http
  seq: 35

http:
  method: DELETE
  url: "{{investment-base-url}}/research-reports/:reportId"
  params:
    - name: reportId
      value: "{{investment-research-report-id}}"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

