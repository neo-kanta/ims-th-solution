info:
  name: get-allocation
  type: http
  seq: 33

http:
  method: GET
  url: "{{investment-base-url}}/funds/:fundId/allocation"
  params:
    - name: fundId
      value: "{{investment-fund-id}}"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
