info:
  name: get-import-batch
  type: http
  seq: 6

http:
  method: GET
  url: "{{market-data-base-url}}/import-batches/:batchId"
  params:
    - name: batchId
      value: "{{market-data-import-batch-id}}"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
