info:
  name: list-session
  type: http
  seq: 6

http:
  method: GET
  url: "{{iam-admin}}/users/:id/sessions"
  params:
    - name: id
      value: a0000000-0000-0000-0000-000000000001
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
