info:
  name: get-email-outbox-detail
  type: http
  seq: 2

http:
  method: GET
  url: "{{notification-base-url}}/email-outbox/:outbox_id"
  params:
    - name: outbox_id
      value: "{{notification-outbox-id}}"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
