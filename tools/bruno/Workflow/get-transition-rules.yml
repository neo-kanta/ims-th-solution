info:
  name: get-transition-rules
  type: http
  seq: 10

http:
  method: GET
  url: "{{base-url}}workflow/transition-rules"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
