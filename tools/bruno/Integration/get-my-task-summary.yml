info:
  name: get-my-task-summary
  type: http
  seq: 11

http:
  method: GET
  url: "{{base-url}}integration/tasks/my/summary"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

