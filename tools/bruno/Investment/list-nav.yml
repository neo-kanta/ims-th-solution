info:
  name: list-nav
  type: http
  seq: 31

http:
  method: GET
  url: "{{investment-base-url}}/portfolios/:portfolioId/nav?from&to&page&limit"
  params:
    - name: portfolioId
      value: "{{investment-portfolio-id}}"
      type: path
    - name: from
      value: "{{investment-from-date}}"
      type: query
    - name: to
      value: "{{investment-to-date}}"
      type: query
    - name: page
      value: "{{investment-page}}"
      type: query
    - name: limit
      value: "{{investment-limit}}"
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

