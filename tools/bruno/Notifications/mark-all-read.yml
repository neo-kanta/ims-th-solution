info:
  name: mark-all-read
  type: http
  seq: 3

http:
  method: POST
  url: "{{notification-base-url}}/read-all"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
