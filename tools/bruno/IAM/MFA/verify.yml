info:
  name: verify
  type: http
  seq: 2

http:
  method: POST
  url: "{{iam-auth}}/verify"
  body:
    type: json
    data: |-
      {
        "totp_code": "string"
      }

      // totp code
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
