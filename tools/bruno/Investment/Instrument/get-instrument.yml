info:
  name: get-instrument
  type: http
  seq: 19

http:
  method: GET
  url: "{{investment-base-url}}/instruments/:instrumentId"
  params:
    - name: instrumentId
      value: "{{investment-instrument-id}}"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
