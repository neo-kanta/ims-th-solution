info:
  name: cancel-request
  type: http
  seq: 6

http:
  method: POST
  url: "{{approval-base-url}}/requests/:requestId/cancel"
  params:
    - name: requestId
      value: "{{approval-request-id}}"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
