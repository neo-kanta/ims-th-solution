info:
  name: get-latest-nav
  type: http
  seq: 37

http:
  method: GET
  url: "{{investment-base-url}}/funds/:fundId/nav/latest"
  params:
    - name: fundId
      value: "{{investment-fund-id}}"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
