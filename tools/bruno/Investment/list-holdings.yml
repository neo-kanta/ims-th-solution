info:
  name: list-holdings
  type: http
  seq: 17

http:
  method: GET
  url: "{{investment-base-url}}/portfolios/:portfolioId/holdings"
  params:
    - name: portfolioId
      value: "{{investment-portfolio-id}}"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

