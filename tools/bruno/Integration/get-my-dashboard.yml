info:
  name: get-my-dashboard
  type: http
  seq: 9

http:
  method: GET
  url: "{{base-url}}integration/dashboard/me"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

