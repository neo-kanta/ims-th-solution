info:
  name: list-trade-confirmations
  type: http
  seq: 1

http:
  method: GET
  url: "{{investment-base-url}}/trade-confirmations?execution_id={{investment-execution-id}}"
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();
        const item = body?.data?.items?.[0] || {};

        if (item.id) {
          bru.setEnvVar("investment-trade-confirmation-id", item.id, { persist: true });
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
