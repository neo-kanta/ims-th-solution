info:
  name: disable
  type: http
  seq: 4

http:
  method: POST
  url: "{{iam-auth}}/disable"
  body:
    type: json
    data: |-
      {
        "totp_code": "string"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
