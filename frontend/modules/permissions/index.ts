import { defineNuxtModule } from 'nuxt/kit'

/**
 * Permissions Management module — accounts, groups, function/data permissions.
 * Auto-discovered by Nuxt from the modules/ directory.
 */
export default defineNuxtModule({
  meta: {
    name: 'permissions',
    configKey: 'permissions',
  },
  setup(_options, _nuxt) {
    // Module setup — add composables auto-imports, components, etc. here
  },
})
