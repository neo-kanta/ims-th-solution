info:
  name: delete-portfolio
  type: http
  seq: 33

http:
  method: DELETE
  url: "{{investment-base-url}}/portfolios/:portfolioId"
  params:
    - name: portfolioId
      value: "{{investment-portfolio-id}}"
      type: path
  body:
    type: json
    data: |-
      {
        "expected_version": {{investment-portfolio-version}}
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

