info:
  name: verify
  type: http
  seq: 2

http:
  method: POST
  url: "{{iam-mfa}}/verify"
  body:
    type: json
    data: |-
      {
        "totp_code": "string"
      }

      // totp code
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
