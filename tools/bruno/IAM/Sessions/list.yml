info:
  name: list-sessions
  type: http
  seq: 1

http:
  method: GET
  url: "{{iam-auth}}/sessions"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
