info:
  name: logout-all
  type: http
  seq: 2

http:
  method: POST
  url: "{{iam-auth}}/logout-all"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
