info:
  name: list-processes
  type: http
  seq: 1

http:
  method: GET
  url: "{{approval-config-base-url}}/processes?process_type={{approval-process-type}}&active_only={{approval-active-only}}"
  params:
    - name: process_type
      value: "{{approval-process-type}}"
      type: query
    - name: active_only
      value: "{{approval-active-only}}"
      type: query
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();
        const rows = Array.isArray(body?.data) ? body.data : [];
        const first = rows[0] || {};

        if (first.id) {
          bru.setEnvVar("approval-process-id", first.id, { persist: true });
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
