info:
  name: get-portfolio-summary
  type: http
  seq: 16

http:
  method: GET
  url: "{{investment-base-url}}/portfolios/:portfolioId/summary"
  params:
    - name: portfolioId
      value: "{{investment-portfolio-id}}"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

