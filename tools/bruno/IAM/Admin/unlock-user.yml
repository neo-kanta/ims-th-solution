info:
  name: unlock-user
  type: http
  seq: 8

http:
  method: POST
  url: "{{iam-admin}}/users/:id/unlock"
  params:
    - name: id
      value: "{{user_id}}"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
