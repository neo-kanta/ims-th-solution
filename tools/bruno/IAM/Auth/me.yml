info:
  name: me
  type: http
  seq: 5

http:
  method: GET
  url: "{{iam-auth}}/me"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
