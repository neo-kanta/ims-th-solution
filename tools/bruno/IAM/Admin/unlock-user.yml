info:
  name: unlock-user
  type: http
  seq: 8

http:
  method: POST
  url: "{{iam-admin}}/user/:id/unlock"
  params:
    - name: id
      value: a0000000-0000-0000-0000-000000000001
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
