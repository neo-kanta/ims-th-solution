info:
  name: delete-item
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{watchlist-base-url}}/items/:id"
  params:
    - name: id
      value: "{{watchlist-item-id}}"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
