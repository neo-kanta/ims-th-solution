info:
  name: get-user-list
  type: http
  seq: 9

http:
  method: GET
  url: "{{iam-admin}}/users?search=&is_active=&is_locked=&offset=0&limit=50"
  params:
    - name: search
      value: ""
      type: query
    - name: is_active
      value: ""
      type: query
    - name: is_locked
      value: ""
      type: query
    - name: offset
      value: "0"
      type: query
    - name: limit
      value: "50"
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
