info:
  name: list-my-tasks
  type: http
  seq: 10

http:
  method: GET
  url: "{{base-url}}integration/tasks/my"
  params:
    - name: module
      value: ""
      type: query
    - name: priority
      value: ""
      type: query
    - name: status
      value: ""
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

